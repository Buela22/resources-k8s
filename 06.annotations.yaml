apiversion: v1
kind: pod
metadata:
  name: annotations-demo
  annotations:
    jenkins-url: "http://jenkins.joindevops.com/build/job/s"
    imageregistry: "https://hub.docker.com/"
spec:
  containers:
    - name: nginx
      image: nginx