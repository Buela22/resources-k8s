apiversion: v1
kind: pod
metadata:
  name: resources-demo
spec: 
   containers:
   - name: nginx
     image: nginx
     resources:
      requests:
        memory: "68Mi"
        cpu: "100m" # 1/10th of cpu
      limits:
        memory: "100Mi"
        cpu: "120m"