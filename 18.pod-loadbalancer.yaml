apiversion: v1
kind: pod
metadata:
  name: service-lb-demo
  #these labels are used to register this pod with service
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: LoadBalancer
spec:
  containers:
  - name: nginx
    image: nginx