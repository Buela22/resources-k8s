apiversion: v1
kind: pod
metadata:
  name: service-np-demo
  #these labels are used to register this pod with service
  labels:
    project: roboshop
    component: frontend
    environment: dev
    purpose: node-port
spec:
  containers:
  - name: nginx
    image: nginx
    